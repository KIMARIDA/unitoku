import Foundation
import CoreData
import SwiftUI

// MARK: - CoreData Type Aliases
// 이 파일은 CoreData 모델에 대한 타입 별칭(type alias)을 제공합니다.
// 다른 모델 정의와의 이름 충돌을 방지하기 위해 사용됩니다.

// Remove type aliases and use direct references to the Core Data entities
// This resolves naming conflicts with the custom struct models in BoardModels.swift

// MARK: - 사용 지침
// CoreData를 사용하는 코드에서 Post와 Comment 대신에 직접 이름을 사용하세요
// CoreData 엔티티를 사용하는 부분은 변경하지 않아도 됩니다.
